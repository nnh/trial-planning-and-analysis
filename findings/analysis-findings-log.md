# 解析の工程の欠陥事例

作成日：2026-08-22
改訂日：2026-09-06

固定データを受け取ってから納品までの間に見つかった欠陥を型として残す。次の試験で同じ箇所を見るための蓄積であり、解析を1本通すたびに追記する。各項目は、何が起きたか・どう見つけるか・出どころの順に書く。

手順の正本は [../pipeline/analysis-pipeline-plan.md](../pipeline/analysis-pipeline-plan.md)。立案時に潰せる型は [../review/planning-review/findings-log.md](../review/planning-review/findings-log.md)、電子症例報告書の設計に由来する型は [../review/ecrf-review/findings-log.md](../review/ecrf-review/findings-log.md) が持つ。ここには、解析の段階で初めて現れる型を置く。

出どころの試験名は匿名化し、試験Aと表記する（[../review/planning-review/findings-log.md](../review/planning-review/findings-log.md) の試験Aと同じ試験を指す）。被験者単位の情報は書かない。症例番号・日付・検査値は型の説明に要らない。要るのは件数と構造である。

## 静かに壊れるもの

いずれもプログラムはエラーを出さずに通り、結果だけが誤る。ログはきれいなまま残る。

### 計画書が名指しする値が実データに存在しない

統計解析計画書が操作的定義で特定の値（識別子・検査コード・訪問番号）を名指ししているのに、その綴りが実データに無い。条件は該当0件で通るためエラーにならず、集計結果が空になるだけである。

固定データを受け取った時点で、計画書が名指しする値を1つずつ実データと照合する。実装後には気づけない。

出どころ：試験A（2026-08-09・2026-08-16）

### 並び順が文字符号化に依存する

行の水準を表示名の照合順序で並べていると、セッションの文字符号化を変えるだけで並びが変わる。値は1つも変わらないため、系統間の突合でも前後比較でも検出できない。有無の2水準のような単純な並びでも起きる。

試験A では SAS を CP932 から UTF-8 のセッションへ移した際、日本語版の12表・387セルで並びが変わった。英語版は変化なし。値の多重集合は完全一致だった。

並びを順序番号で持ち、表示名に依存させない。移行の前後で成果物を突き合わせるときは、値の多重集合と並び順を別々に見る。

出どころ：試験A（2026-08-21）

### 受領データに1つだけ別の符号化が混ざる

外部から受けるマスタ（施設・疾患・薬剤）は提供元が違うと符号化も違う。CP932 の CSV が1つ混ざると読み込みで列がずれ、後続の変数に別の値が入る。ずれた列が数値なら無効なデータの注意が出るが、文字なら黙って別の値が入る。

入力データの符号化を、ソースと同じように一覧で確かめる。試験ごとの受領データの置き場と、外部マスタの置き場の両方を見る。

出どころ：試験A（2026-08-21）

### 引用符の中の改行でレコードが割れる

自由記載の項目に改行が入っていると、引用の規則を解さない読み込みでレコードが分割される。試験A では24レコードが198行に割れた。

受領仕様に、引用の規則と改行を含むフィールドの有無を書かせる。読み込み後にレコード数を受領時の件数と突き合わせる。

出どころ：試験A（2026-08-09）

### 日付が数値型に読まれる

外部データの日付が引用されていないと、読み込みで数値型になる。値は入るので欠測にはならず、後段で日付として扱ったときに初めて破綻する。

出どころ：試験A（2026-08-20）

### 一括置換でパスを移すと、区切り文字の違う書き方が残る

ディレクトリ構成を変えてパスを一括置換するとき、`docs/` を探すパターンは `docs\` を拾わない。コメントに書かれた説明用のパスだけが直り、コードが読み書きする実体のパスが旧いまま残る。旧いパス同士で閉じていると動作は続くため、テストも通る。

試験Aで2件出た。片方は仕分け表を旧パスから読んでいて、既知として整理済みの指摘が未仕分けとして表示された。もう片方は外部標準の写しを旧い場所へ書き出し、正しい置き場に置いた正本が使われないままだった。どちらもコメントには正しいパスが書かれており、コードだけが取り残されていた。

移した後は、移した先が実際に読み書きされるかを1度は動かして確かめる。ファイルの更新時刻を見れば、書かれた場所が分かる。

出どころ：試験A（2026-08-29）

### 読み込みに失敗した設定が黙って既定値へ倒れる

設定ファイルの読み込みを「あれば読む」で書くと、パスが変わって読めなくなったときにエラーも警告も出ない。空の設定で処理が続き、出力は正常な形のまま中身だけが変わる。

試験Aでは、指摘の仕分け表が読めずに「既知として残すと決めた指摘 0 件、仕分けの対象 26 ルール」と表示された。実際は全件が既知として整理済みで、未仕分けは0件だった。数字が出ているため、見ただけでは失敗と分からない。

処理に必要な外部ファイルは、無ければ止める。「あれば読む」でよいのは、無い状態が正常な場合だけである。

出どころ：試験A（2026-08-29）

### 生成の段が入力を見つけられずに飛ばす

生成のスクリプトが入力を「無ければ飛ばす」で書かれていると、フォルダ構成を変えた日から入力が見つからなくなっても、実行は成功で終わる。作れていないことと、作り直したが中身が変わらなかったことが区別できない。成果物は前に作ったものが残るので、古いまま配布物へ入り続ける。

試験Aで、同じ日の構成変更の取り残しとして2件出た。1つは定義の表示用 HTML を作る段で、参照するフォルダが構成の変更で無くなっており、数週間にわたって毎回「定義が無いので飛ばす」で終わっていた。もう1つは定義の更新そのもので、対応表の置き場が変わったために毎回そこで止まり、定義が数週間前の版のまま納品物に入っていた。

生成に必要な入力は、無ければ終了コードを非0で落とす。前項（設定が既定へ倒れる型）と規則は同じで、現れ方が違う。前項は中身だけが変わり、こちらは何も作られない。構成を変えた日は、各生成の段が実際に何かを書いたかを、成果物の更新時刻で1度確かめる。

出どころ：試験A（2026-09-05）

### 同じ名前のファイルが2箇所にあり、片方が古い

読み先の既定を新しい場所へ移しても、旧い場所に同じ名前のファイルが残っていると、置き場を引数で明示したときに古い方を掴む。既定で回しているかぎり正しく動くので、明示した回だけが違う結果になる。

試験Aで、変数メタデータの3本が旧い場所と新しい場所の両方にある状態になった。既定の読み先は新しい場所へ移っていたが、実装系統を切り替えるために置き場を明示する経路が残っており、そこから回すと旧い方を読んだ。

既定を移したら、旧い場所の実体を消す。消す前に、その名前でリポジトリ全体を検索して読む側が残っていないことを確かめ、あわせて当たるはずの別の名前で1件当てて検索そのものが効いていることを確かめる。中身が宣言から作り直せることも確かめておく。

出どころ：試験A（2026-09-05）

### 全体の件数が合っていても、集団で絞ると合わなくなる

報告の件数と解析対象集団の例数が一致していても、集団の内と外で過不足が相殺していることがある。集団外の症例が報告を持ち、集団内の症例が報告を持たない場合、総数だけを数えると気づけない。

試験Aでは、観察終了報告の総件数と解析対象集団の例数がどちらも同じ数だった。内訳を見ると、集団外の2例が報告を持ち、集団内の2例が持っていなかった。集団で絞った表の分母だけが2例少なくなり、表の副題が宣言する例数と食い違っていた。

分母が集団で決まる表は、総数の一致ではなく、集団の識別子と報告の識別子の差集合を両方向で取って確かめる。

出どころ：試験A（2026-08-29）

### 下の層に行が増えると、除外で選んでいた表示がそれを吸い込む

ある層（解析結果データなど）に新しい種類の行を足すと、その層を読む表示が「これとこれを除く」という形で行を選んでいた場合、新しい行が黙って表へ入る。両系統が同じ書き方をしていれば同じように入るので、系統間の突合は不一致0で通る。変わるのはセルの件数だけである。

試験Aで、生存曲線の全イベント時点を解析結果データへ足したところ、指定時点（1〜5年）だけを出すはずの時点表19件が、全イベント時点を印字するようになった。ある表は5行のところ89行になっていた。セル台帳は 13,963 から 17,599 セルへ増えたが、両系統とも同じ結果なので突合は不一致0のままだった。追加を計画した文書自体が「指定時点だけを取り出す形に変える」と書いていたのに、その段が実施されないまま完了として記録されていた。

行の選択は、除外の列挙ではなく採用で書く（「この水準だけを採る」）。そうすれば層に何が増えても表は変わらない。層に行の種類を足したときは、その層を読むすべての表示について選び方を確かめる。突合を通すだけでなく、セル台帳の件数を追加の前後で比べる。件数が動いたら、動いてよい理由が説明できるかを見る。

出どころ：試験A（2026-08-29）

### 統計オプションを既定に任せた箇所が、層ごとに違う方式になる

信頼区間の変換のように、処理系が既定値を持つ統計オプションがある。方式を決めても、それを明示していない箇所は処理系の既定で計算される。表と図で違う方式、系統間で違う方式が同時に成立する。図はセル台帳を持たないため、系統間の突合では捕まらない。

試験Aは信頼区間を線形形式（分散をそのまま用いる形）と決めていた。明示していたのは表と解析結果データを作る箇所だけで、図を描く箇所は両系統とも既定のままだった。処理系ごとに既定が違うため、一方は log-log 変換、もう一方は log 変換で帯が描かれていた。差は小さくない。指定時点で下限が 1.3〜2.0 ポイント、全イベント時点の最大では 4.4 ポイント動く（推定値が1に近いほど離れる）。主要評価項目の下限が、図では 60.3%、表では 61.6% と食い違っていた。判定の結論は変わらなかったが、同じ図表番号の中で図と表が違う数字を示していた。

方式を決めたら、それを出すすべての箇所で明示する。決定を記録する文書に「どの箇所に効くか」（表・解析結果データ・図・Excel × 系統）を書き、箇所の数を数えて確かめる。既定に任せた呼び出しは、オプション名が書かれていないことで機械的に見つかる。

出どころ：試験A（2026-08-29）

### 部分集団の識別子が格納長で切れ、宣言と食い違う

固定長の文字変数を持つ処理系は、宣言した幅を超えた文字を黙って捨てる。ログにも出ない。可変長の処理系は切らないので、同じ識別子が系統ごとに別の値になる。構文検査も通り、結果値の突合も通る。止まるのは宣言と実装の値を照らす段だけで、そこまで来ないと分からない。既存の識別子が短いうちは現れず、長い識別子を1つ足した日に初めて出る。

試験Aでは、2つの治療相をまとめた部分集団の識別子（22文字）が、20バイトで宣言された列で20文字へ切れた。通し実行は「宣言に無い部分集団が結果データで使われている」で止まったが、そこから識別子の長さへ遡る道は自明でない。同じ列の宣言は共通マクロの中に8箇所あり、1つでも狭いものが残れば、そのマクロを通った行だけが切れる。

識別子を格納長に収める規則を持ち、宣言された識別子が格納長に収まるかを毎回検査する。検査は格納長の数字を自分で持たず、実装のソースの宣言から読む。数字を検査側へ写すと、実装を広げたときに検査だけが古くなる。同じ列の宣言が複数箇所にあるなら、すべて同じ幅であることも併せて見る。検証プログラムの中で同じ機構が働く型は「判定の文字列が変数長で切れ、常に一致と読まれる」。

出どころ：試験A（2026-09-06）

### 表の対象時点と、行が引く変数の時点がずれる

ある時点の状況を示す表なのに、行が別の時点の値を持つ変数を引いている。値そのものは正しく、行数も合うので、系統間の突合もセル台帳も視覚回帰も通る。表が何を示す表かという観点で人が読んで初めて分かる。

試験Aでは、ある治療の開始時の状況を示す表で、ある所見の有無を示す2行が、別の帳票（その治療より前の段階の最終報告）の重症度・病型の変数から来ていた。行の表示名は有無と書いてあるのに、変数が持つのは段階の分類で、表示名と変数の意味も食い違っていた。取り違えの入口は、同じ語が2つの時点の帳票に現れることにある。

行が引く変数の出所（どの帳票のどの時点か）を、表の対象時点と並べて仕様に書く。同じ語が複数の時点に現れる項目は、行の表示名にも時点を入れ、どちらの時点かが行名から読めるようにする。

同じ表について、欠測をどう表示するかを決めた文言も、その行が実際には使っていない水準集合を名指ししていた。文言どおりに実装しても対象の行は変わらず、決定が触れていない別の表にだけ該当0件の行が増える。決定を受けて実装するときは、名指しされた対象が、その行が実際に引いている変数・水準集合と同じかを先に確かめる。

出どころ：試験A（2026-09-06）

## 二重コーディングで捕まるもの

捕まるのは計算違いより受け渡しの欠陥である。片系統の出力を見ているだけでは気づけない3種が繰り返し出る。

### 0例の水準が結果から脱落する

集計で該当が0の水準が、結果データに行として残らない。表に出すときに行ごと消える。図表案が「該当0の水準も出す」と書いていないと、実装で決めることになる。

出どころ：試験A（2026-08）

### 書き出し時に桁が切れる

中間形式へ書き出すときの長さ指定が足りず、表示文言や結果値が途中で切れる。切れた値も文字列としては妥当なので、目で見ないと分からない。試験A では表示文言が400バイトで切れており、表示型側の長さ指定を1箇所直して5表が直った。

出どころ：試験A（2026-08-21）

### 並べ替えキーが効かず識別子がずれる

結果を結合するときの並べ替えが期待どおりに効かず、値と識別子の対応がずれる。値そのものは正しいので、合計や件数では気づけない。

出どころ：試験A（2026-08）

### 割れる箇所は仕様書が規定していない箇所と一致する

同じ仕様書から独立に2実装すると、割れる箇所は仕様書が規定していない箇所とほぼ一致する。突合をデータセットの層まで広げると、検出されるのは計算違いではなく仕様書の網羅性の欠落になる。割れの多さは、その層に仕様書が要ることの証拠として読む。

出どころ：試験A（2026-08）

### 同じ規則を両系統が別の列で表す

両系統が同じ意図の条件を別の列で書いていると、そのときのデータで両者が同じ行集合を指しているかぎり突合は通り続ける。分かれるのは、新しい経路の行が増えたときである。

試験Aでは、ある値を残す行の条件を、片方が区分の列で、もう片方が帳票の識別子の列で書いていた。固定データではその値を持つ行がその帳票にしか無かったため、両者は同じ行集合を指しており、突合は長く通っていた。未提出の帳票を補う外部データの行が増えた日に、片方だけがその行の値を残し、突合が1件落ちた。

意図が同じなら条件式も同じ列で書く。突合が通っていることは、両系統が同じ列で判定している証拠にならない。層に新しい経路の行（外部データ由来、後から足した区分）を入れるときは、その層を読む条件式がどの列を見ているかを両系統で並べて確かめる。ソースの字面が違うこと自体は食い違いではないので（「宣言が食い違っていても、値が合えば突合は通る」の末尾）、見るのは字面ではなく、条件が参照している列である。

出どころ：試験A（2026-09-06）

## 宣言と生成物

宣言の実体を CSV 1つに保つこと、ソースを正規表現で解析する設計を採らないことの根拠は、この2つの型がまさに欠陥として現れた実例である。この2件は設計原則（[../pipeline/analysis-pipeline-plan.md](../pipeline/analysis-pipeline-plan.md)「図表を宣言で駆動する」）に組み込まれ、その原則を守っている限り構造的に再発しないため、ここには残さずそちらへ移した。

### 同じ動的展開が2つ実装される

1つの宣言が複数の表を生む場合（治療相やコースで展開する表）、系統ごとに展開の実装を持つと、展開の条件がずれても結果の表の数が同じなら気づきにくい。展開を表示型の中へ入れ、宣言からは見えない形にする。

既存の表示型ではこの形に揃えて解決済みだが、新しい表示型を追加するときに同じ設計判断を再び踏む可能性があるため、まだ確認の対象として残す。

出どころ：試験A（2026-08-21）

### 生成物どうしの依存で、同じコードでも結果が変わる

トレーサビリティ索引は仕様書 HTML を読み込んでリンクを張る。仕様書を作り直す前に索引を作ると、索引は古い仕様書を見る。同じコードを2回回すと1回目と2回目で結果が変わり、差分を見た人が「コードを変えたせいだ」と読み違える。

試験A の汎用化で実際に起きた。スクリプトの改変前後を突き合わせたところ、11件の生成物のうちトレーサビリティ索引だけが182バイト違った。改変前のコードに戻して回しても同じ差が出たため、原因が改変ではなく生成の順序だと分かった。

生成物に依存があるなら順序を決め、実行スクリプトを順序の正本にする。突き合わせるときは、同じ順序で回した2つを比べる。

出どころ：試験A（2026-08-22）

### 判定の経路を変えても、それを説明する文言は追随しない

導出の規則を変えたとき、実装は直っても、それを説明する文言が改訂前のまま残る。コードのコメントと、図表に印字される副題・脚注が該当する。どちらも実装から自動で生成されないため、値が正しくても説明だけが誤る。

試験Aでは、ある効果判定の導出を判定欄から測定値へ改めた後、図表の副題が「判定欄から採る」と書いたまま9日間残った。印字される文言なので、読み手には誤った導出が伝わる。実測の例数が改訂前後で違っていたため、数を突き合わせて気づいた。

判定の経路を変えたら、その判定を説明している箇所を同時に探す。実装・コメント・図表のラベル・仕様書の4つが対象になる。改訂前後で値が変わる導出なら、両方の値を記録に残しておくと、後から説明の食い違いを数で検出できる。

出どころ：試験A（2026-08-29）

### 品質管理のメモが成果物の表示ラベルに残る

未決事項の番号、暫定、要見直しといった内部の管理メモを表示文言のカタログへ書くと、そのまま図表へ印字され、納品物に載る。カタログは成果物の一部であり、作業用の覚え書きの置き場ではない。

試験Aでは、5件の副題に「未決事項 B-6」「暫定値」「見直しが要る」が残ったまま、研究者へ渡すパッケージへ入っていた。機械検査はいずれも通っている。文言の中身を見る検査が無いためである。

カタログの文言は、外へ出る文章として書く。未決の状態は別の管理表が持つ。カタログに管理用の語が現れないことを、禁止語の一覧で検査できる。

出どころ：試験A（2026-08-29）

### 仕様索引が持つと書く行を実装が作らず、検査は実装から宣言の向きしか見ない

宣言に無い解析が結果値に出たら止める検査はあるが、宣言（仕様索引）にあって結果値に無い解析を拾う検査が無いと、図表案の行が黙って欠ける。索引の文章が「併記する」と書いていれば、読む側は在ると思う。

試験Aで、SAP の図表案が持つ併記行（芽球の推移、治療相別の死亡割合など）を仕様索引が「持つ」と書いたまま、結果値には無かった。

宣言と結果値の照合は両向きで行う。索引の各解析について結果値の行数を数え、0 の解析を列挙する。

出どころ：試験A（2026-09-06）

### 副題に直書きした件数が、評価項目の値の唯一の置き場になる

分母を件数にした表の断りとして症例数と割合を副題に書くと、その値は結果値（ARD）にも索引にも無く、表示文言のカタログにだけ在ることになる。データが変わっても追随せず、評価項目として規定された値なのにトレーサビリティの外にある。

試験Aで、重篤な有害事象の症例割合（研究計画書の副次評価項目）が表の副題の文字列にしか無かった。

表示文言に数値を書かない。数値が要る文言は結果値から差し込む。評価項目の一覧と結果値の解析の対応を検査する。

出どころ：試験A（2026-09-06）

## 検証側の誤り

### 検査が差分を見つけても止まらない

突合や検査のスクリプトが差分・欠落を検出しても、報告を書くだけで終了コードを0のまま返す。呼び出し側は成功として次の段階へ進む。ログには差分が記録されているが、誰も読まない。

試験Aでは、SDTM・ADaM・図表の突合3本がこの形だった。片方の系統からデータセットが丸ごと欠落しても、比較の記録に「欠落」と書かれるだけで実行は成功した。主要評価項目の突合1本だけが非ゼロで終了する作りになっており、他は報告で終わっていた。

検査を書いたら、失敗させる条件を先に決め、その条件で実際に失敗することを確かめる。行を増やす、時点を削る、参照を空にする、データセットを1つ消すといった変異を入れて、検査が止まることを見る。止まらない検査は、通っても意味がない。

出どころ：試験A（2026-08-29）

### 検査が走っていないのに0件と読む

機械検査が入力の想定外の値で異常終了すると、以降の規則が1つも走らない。終了コードを見ていないと、0件と区別が付かない。

規則ごとの件数を出させ、件数が出ていることをその規則が走った証拠にする。

出どころ：試験A（2026-08-22）

### 探索コマンドが実行されないまま空を返す

存在しないコマンドで探索を包むと、コマンドは実行されないまま空の出力を返す。標準エラーを捨てていると、0件のヒットと区別が付かない。ラッパーの不在は本体の不在と違い、コマンドの意味を変えずに実行そのものを消す。

無いと報告する前に、その検索が実行された肯定的証拠を取る。終了コード、件数、走査対象のファイル数のいずれか。あわせて、対象が在る場所を1件は肯定的に確認する。

出どころ：試験A（2026-08-16）

### 出力のハッシュ比較が可変部で失敗する

改変の前後で出力が変わっていないことを示すとき、単純なハッシュ比較は失敗する。実行ごとに変わる可変部（生成日時、ヘッダの作成時刻）が埋め込まれているため。何を接地点にし、何を正規化するかを先に決める。

出どころ：試験A（2026-08-21）

### 入力誤りの型を仮定すると、影響の見積もりが変わる

データに入力誤りの疑いがあるとき、どの型の誤りかを決めないまま影響を見積もると、結論が変わる。桁の欠落を想定するか、小数点の位置を想定するかで、修正後の値が閾値のどちら側に来るかが違う。

試験Aでは、規定値の入力誤りを疑って照会を出す際、規定値そのものへの修正を想定して「どちらでも判定は変わらない」と記録した。実際の修正は桁が2つ違う値で、判定が1区分動いた。該当したのは1症例2件の測定値で、5つの解析の集計が変わった。

影響なしと見積もれるのは、誤りの型を確定できた場合に限る。型が決まらないうちは、想定しうる型ごとに判定を当てて、結論が変わらないことを確かめる。

出どころ：試験A（2026-08-29）

### 否定を確かめるときだけ確認が浅くなる

同じセッションの中で、ある処理系は導入先の実在まで確かめ、別の処理系は `command -v` の空振り1回で「無い」と結論することがある。違いは目的にある。使うために探すときは見つかるまで探すが、無いと報告するために打つときは、否定的な結果が出た時点で確認が止まる。

試験Aで、解析の二重コーディングに使う処理系について「この端末に入っていない」と報告した。実際には環境の方針文書が導入先とフルパスでの呼び方を定めており、端末ごとの記録にも導入済みと書かれていた。どちらもプロジェクトの規約が正本として名指ししている文書で、読めば1分で分かる。

実害は報告の誤りだけでは終わらない。誤った前提が積み残しの一覧に書き込まれ、「環境をどうするか決めてほしい」という存在しない判断を関係者に繰り返し求めることになった。一覧に載った時点で、それは共有された事実として扱われる。

環境の有無を判断する前に、方針文書と端末ごとの記録を読む。既定の導入先を直接見る。PATH の空振りは不在の証拠にならない。**否定を確認しに行くときほど深く見る。**

出どころ：試験A（2026-08-29）

### 突合の相手が古いまま差分として現れる

二重コーディングの突合で、比べる材料（セル台帳・中間ファイル）が複数系統あり、そのうち1つだけを作り直して突合すると、更新した側と古い側が並ぶ。差分は出るが、それは系統間の不一致ではなく、作った時点の違いである。

試験Aで、当日直した4つの表に13セルの差が出た。中身を見ると、片方が修正後、もう片方が修正前の値だった。台帳は3系統あり（描画Aと数値A、描画Bと数値B、描画Bと数値A）、既定の突合相手は3つ目だが、それを作る実行を回していなかった。作り直したところ不一致0になった。

あわせて、その3つ目を作る実行が配布物まで書き換えることに後から気づいた。同じスクリプトが台帳と図表の両方を書くため、突合の材料を作る目的で回すと、配る側の図表が別の数値系統から描かれたものに変わる。値が一致していても出所が変わる。

差分を見たら、まず両側の生成時刻を確かめる。突合は「同じ時点の材料どうし」でしか意味を持たない。材料を作る実行が他に何を書くかを、回す前に読む。複数系統の材料を使う突合は、作る順序と最後に何を作り直すかまで手順に書く。

出どころ：試験A（2026-08-29）

### 同じ値から別の突合キーが作られる

数値を文字列にして突合キーにするとき、系統ごとに既定の桁が違うと、同じ値から別のキーができる。値そのものは一致しているのに、キーが合わないため全行が「片側にしかない行」として現れる。

試験Aで、生存曲線の時点をキーにしようとした。一方の処理系は既定で12桁、もう一方は7桁の文字列を作る。同じ 0.0793976728 という値から、末尾の桁数が違う2つのキーができていた。そのまま突合すれば数百件の不一致が出て、原因が値の違いではなく書式だと気づくまで時間がかかる。実装の途中で気づいたので実害は出ていない。

連続値をキーにするなら、丸めの桁と出力書式を両系統で固定し、同じ入力から同じ文字列が出ることを数例で実測してから使う。既定値に任せない。丸めの桁は、元データの精度から決める（日単位の値を年へ換算したなら、小数第6位で秒未満まで表せる）。

そもそも連続値をキーにしない設計も採れる。時点に通し番号を振り、値は別の統計量として持つ形である。ただし通し番号は両系統で同じ順序に並ぶことが前提になるので、どちらの前提が確かめやすいかで選ぶ。

出どころ：試験A（2026-08-29）

### 生成したコードにバックスラッシュのエスケープが残る

スクリプトを別のスクリプトから書き出すとき、Windows のパスに含まれるバックスラッシュがエスケープとして解釈され、書き出した先に制御文字が入る。`\b` はバックスペース、`\t` はタブ、`\n` は改行になる。

試験Aで、パイプラインの実行スクリプトへ `scripts\build-ars-json.py` と書いたところ、`\b` が制御文字（U+0008）に化けた。ファイル上は不可視で、`grep` では `scriptsuild-ars-json.py` と見える。実行時のエラーは `x08` を含む読みにくいもので、原因に辿り着きにくい。

**このとき全体は成功したように見える。** 追加した処理だけが失敗し、元からある段はすべて通るため、終了コードもエラー件数も正常を示す。新しく足した処理が実際に走ったかは、成果物の有無で別に確かめる。

同じ日に4回起きた。パスの一括置換で区切り文字が漏れた件、納品物に残ったローカルパスの件、この件、そして別の言語のソースへ正規表現を書き出した件である。最後の1件はパスではない。`\b`（単語境界）を含むパターンを書き出したところ、書き出す側の文字列リテラルで解釈されて制御文字になり、パターンから境界の指定だけが消えた。パス以外でも同じことが起きる。

コードを生成するときはバックスラッシュを書かない。パスの連結はその言語の関数に任せる（PowerShell なら `Join-Path` に引数を分けて渡す、Python なら `os.path.join`）。どうしても必要なら、文字コードで書く（`chr(92)`）か、生文字列を使う。書き出した後は、制御文字が入っていないかを機械で走査する。

出どころ：試験A（2026-08-29）

### 規格の性質を一次情報に当たらずに書き、それが設計の前提になる

外部の標準について「この標準はここまでは規定していない」と書くと、その一文が以降の設計判断の前提として働き続ける。誤っていても、規格を読み直すまで誰も検証しない。準拠しない選択が、判断された結果ではなく既定として残る。

試験Aの方法論文書に「標準が規定しているのは解析メタデータの層だけで、結果値の物理的な列名や型は規定していない。したがって準拠かどうかは判定できる問いではない」と書いていた。誤りである。標準は結果値のクラスを規定しており、必須スロットも定めている。同じ文書の別の節では、そのクラスを主要クラスとして列挙していた。文書の中で矛盾していたが、13日間気づかなかった。この一文があるために、機械可読な成果物を作ることが「該当する試験だけの追加作業」に分類され、準拠は最初から見送られていた。一次情報に当たって書き直したところ、不足していたのは必須スロット3つだけで、識別子の体系はもともと対応していた。

外部の標準に言及するときは、クラス名・スロット名・必須の別・列挙値を一次情報（規格が公開しているモデルの記述）から写し取る。要約や記憶で書かない。とくに否定形（「規定していない」「できない」）は、設計の選択肢を閉じるので、肯定形より強い根拠が要る。同じ文書の中に矛盾が生じていないかも見る。標準の性質を述べた文には、いつ何を見て書いたかを併記する。

出どころ：試験A（2026-08-29）

### 自前で書いた適合性の検査を、規格への準拠の根拠にする

外部標準に準拠したかを、自前で書いた検査で判定する。必須項目の一覧を要約や記憶から書き起こすため、検査は書いた分しか見ない。通っても準拠の証拠にならないが、通ったという事実が準拠の記録として残る。

試験Aで、ARS 準拠の成果物を作り、必須スロットを5個だけ見る検査を突合スクリプトに書いて「準拠」と記録した。その後、標準が公開している JSON-Schema にかけたところ、6種類・1,319件の必須スロット漏れが出た。漏れていたのはいずれも構造上のスロット（結果を群ごとに報告するかの真偽値、表示の並び、集団と部分集合の階層と順序、群がデータ由来かの真偽値、目次が入れ子の型であること）で、試験の側の判断を要さないものばかりだった。値の誤りはなく、系統間の突合も是正の前後で不一致0のままだった。つまり、この種の漏れは突合でも自前の検査でも見えない。

規格が機械可読なスキーマを公開しているなら、それにかける。自前の検査は、スキーマが表せない試験固有の制約（値の対応関係、識別子の体系）に限る。スキーマの写しはリポジトリに置き、実行のたびに網へ取りに行かない（検査の結果が外の変更で動かないようにする）。検証に外部のライブラリが要る場合は、入っていない環境では合否と区別できる終了コードで「検証できなかった」と返す。黙って通すと、検査を持っているのに動いていない状態が残る。

出どころ：試験A（2026-08-29）

### 存在しない引数名を渡すと、設定が黙って無視される

関数が `...` を持つと、綴りを間違えた引数名や、その関数に無い引数名がそこへ吸い込まれる。エラーも警告も出ず、既定値のまま処理が進む。生成物はできるので、開いて見るまで気づかない。

試験Aで、表計算のチャートの種類を指定する引数名を取り違えた（正しくは `style`、渡したのは `scatterstyle`）。既定の「点だけ」のままになり、生存時間曲線の線が1本も描かれない図が納品パッケージまで入った。ファイルは正常に開き、XML も妥当で、機械検査もすべて通っていた。同じ図で、欠測を空文字列として書いたために表計算がそれを0と読み、打ち切りの系列が横軸に沿って並ぶ不具合も同時に起きていた。どちらも人が図を見て初めて分かった。

外部のパッケージに設定を渡すときは、渡した値が実際にオブジェクトへ入ったかを1度確かめる（設定した直後にオブジェクトの中身を表示する）。`...` を持つ関数では、引数名の綴りの誤りは検出されない。そして生成物は画像として見る。表・数値は機械で突き合わせられるが、図の見え方を確かめる方法は目で見ることしかない。

出どころ：試験A（2026-08-29）

### マクロ変数を含む表示文言が、展開されないまま印字される

表示文言のカタログに処理系のマクロ変数（`&ph` のような）を埋め、描画のときに値へ置き換える
設計にしていると、置き換えを止める別の手当てが後から入ったときに、記号のまま印字される。

試験A では、文言を返すマクロに `%superq` を入れた。表示文言に `%` を含むもの（95%信頼区間）が
マクロ呼び出しとして解釈されて落ちるための対策だったが、同時にマクロ変数の解決も止まった。
表題18ブロックが `有害事象 &ph &tk` のまま出力され、9日以上気づかれなかった。もう一方の系統は
置き換えを明示的に書いていたため正しく出ており、系統間の突合は題名を比べないので検出しない。

表示文言に処理系の記法を埋めるなら、置き換えを明示的な関数で行い、カタログの文言に記法が
残っていないことを検査する。文言の中身を見る検査が無いと、記号がそのまま配布物へ出る。

出どころ：試験A（2026-08-30）

### 図の軸ラベルを全図で共通にすると、起算日の違う図で嘘になる

生存時間の図の横軸を、表示文言のカタログの1項目で全図に当てていると、起算日が図ごとに違う
場合に誤った説明が付く。試験A では、無再発生存の図の起算日が寛解到達日であるのに、横軸が
「登録からの期間」と書かれていた。副題は正しく「起算日は LFS 到達日」と書いており、軸だけが
共通の既定文言のままだった。

図ごとに文言を差し替えられる仕組みを持ち、既定は共通のものを使う。機械検査では拾えないので、
図を実際に描いて人が見る工程が要る。

出どころ：試験A（2026-08-30）

### 判定の文字列が変数長で切れ、常に一致と読まれる

検査プログラムの中で判定結果を文字列で持つとき、宣言した長さが足りないと文字が途中で切れる。
比較の相手と一致しなくなるので、判定が常に片側へ倒れる。試験A では判定変数を8バイトで宣言し、
UTF-8 で9バイトの「不一致」が切れて、不一致でも一致と判定されていた。

文字列で判定を持つなら、比較する値の最大バイト長を数えて宣言する。あるいは判定を数値の
フラグで持ち、文字列は表示にだけ使う。実際に条件へ触れさせて、検査が止まることを確かめる。

出どころ：試験A（2026-08-30）

### 古い突合結果が、現行の合否として配布物に残る

突合の結果をファイル名で区別している場合、命名を変えたときに古い版が残る。配布物を組み立てる
処理が名前の型で拾っていると、古い結果が現行の合否として入る。試験A では、10日前の突合結果
（8,991行の不一致）が納品物に同梱されていた。

生成物は日付や実行の識別子を名前に持たせ、配布へ入れるものは実在する最新の1組だけを選ぶ。
選ぶ根拠を名前の型に置かず、対になる材料の実在で決める。

出どころ：試験A（2026-08-30）

### 参照を辿って集める仕組みに、外へ出す境界が無い

同梱する文書を「コードのコメントが指すもの」から集め、その文書が指す文書も辿ると、一覧を別に持たずに済む代わりに、辿る先に境界が無い。仕様が調査の記録を指し、記録が作業ログを指すので、芋づる式に内部文書が配布物へ入る。生成は成功し、リンクの検査も通る（参照先が同梱されているので当然に通る）。

試験Aの納品パッケージに、データセンターへの照会メール案2件（担当者の実名と送受信の時刻を含む）、独立レビューの実施記録、統計解析計画書本体への修正指示書、未決事項の一覧、日次の作業ログ2件が入っていた。合計9件。研究者が読む前提で書かれていない文書ばかりで、修正指示書に至っては固定版の計画書と食い違いうるため、渡せば「どちらが正しいのか」という問いを生む。

辿って集める設計を採るなら、外へ出してよい範囲をディレクトリで宣言し、既定を「出さない」にする。仕様と規制文書のフォルダは全部、調査の記録は名前を挙げたものだけ、というように。名前で挙げるのは、新しい記録を足したときに黙って配布物へ入らないようにするためである。

除くだけでは足りない。同梱する文書が除いた文書を指していると、配布物の中で参照先が無くなり、リンクの検査が今度は落ちる。同梱するときにリンクを外して素の文字列にし、同梱していない旨を断る。外した文書は実行のたびに一覧で報告する。黙って外すと、次に必要な記録まで落ちたことに気づけない。

出どころ：試験A（2026-08-29）

### 呼び出し側が子プロセスの終了コードを見ず、出力を末尾で絞る

実行の入口が段を呼ぶとき、子プロセスの終了コードを見ずに次へ進み、あわせて画面へ出す量を末尾の数行に絞っていると、失敗が画面からも判定からも消える。段そのものは正しくエラーを出しているので、ログを開けば書かれている。誰も開かないだけである。

試験Aで、定義の更新が毎回失敗していたのに、呼び出し側が終了コードを見ておらず出力も末尾5行に絞っていたため、通し実行は成功で終わっていた。同じ形が同じリポジトリの別の場所にもあり、結果メタデータを作る段の終了コードも見られていなかった。

出力を絞ることと、止めることを分ける。標準出力だけを捕まえて末尾を出し、標準エラーは素通しにし、終了コードは必ず見る。これは「検査が差分を見つけても止まらない」の裏返しである。検査が正しく非0を返しても、呼ぶ側が見なければ結果は同じになる。検査を書いたら、それを呼ぶ側が非0で止まることまで実際に確かめる。

出どころ：試験A（2026-09-05）

### 検査が対象を拡張子で探す

検査が対象をファイル名の拡張子で見つけていると、同じ役割のものが別の言語で書かれたとたん網から外れる。検査は正常に走り、指摘0で終わる。走ったが対象が1件も無かったことと、対象を見て問題が無かったことが区別できない。

試験Aで、同梱文書が案内するコマンドの引数を照合する検査が、対象のスクリプトを1つの拡張子で見つける作りだった。実行の入口を別の言語からその言語へそろえた日に、初めてその入口が検査の対象になり、それまで見逃されていた誤りが2件出た。どちらも文書の側が他のスクリプトの引数を案内文と同じ文の中に置いており、読み手には案内しているスクリプトがその引数を受け取ると読める書き方だった。

検査は対象を役割で見つける。拡張子や名前の型で絞るなら、絞った結果の件数を出させ、その件数が対象の全体と合うことを別に確かめる。配布物に新しい形式や新しい言語を足すときは、どの検査に掛かるかを先に確かめる。登録の無い種別は、どの検査にも掛からないまま配られる。

出どころ：試験A（2026-09-05）

### 宣言が食い違っていても、値が合えば突合は通る

二重コーディングの突合が結果の値だけを鍵にしていると、両系統が同じ解析へ別の宣言（どの集団を分母にしたか、どの部分集団で絞ったか）を渡していても、値が一致していれば通る。宣言の食い違いは差分として現れない。

試験Aで、結果データの突合の鍵は6つあり、解析対象集団と部分集団を含んでいなかった。鍵にこの2つを足したところ、両系統が同じ解析へ別の部分集団を渡している箇所が見えた。値は一致していたので、それまでの突合はすべて不一致0だった。

実装が宣言として渡す属性は、突合の鍵に入れる。鍵に入らない属性は、突合ではなく、宣言と実装を照らす別の検査（受入基準との照合）で見るしかない。どちらにも入っていない属性は誰も見ていない。

あわせて、この経緯では「両系統でソースの書き方が違う」ことと「書き出された結果が違う」ことを取り違えた記録が一度残った。片方の実装が識別子の接頭辞を書き出しの時点で補っていたため、ソースの字面は違っても成果物は一致していた。食い違いを報告するときは、ソースではなく突合の対象になる成果物で確かめる。

出どころ：試験A（2026-09-05）

### 環境が足りない端末で、検査が0件のまま通る

ブラウザや外部の検証エンジンを要する検査は、それが入っていない端末では対象を1件も見ないまま、成功と同じ終了コードで終わる。検査の対象が無いのではなく、検査そのものが動いていない。手で回している間は気づかないが、通し実行の段階に入れた日から、その端末では段が黙って通るようになる。

試験Aで、図表をブラウザで開いて見た目を照合する検査が、ブラウザの入っていない端末で終了コード0を返す作りだった。同じ形の欠陥を一度は視覚回帰の側で見つけて閉じており、索引の確認の側に同じ形が残っていた。

検査には、飛ばした件数と見た件数を出させる。飛ばす口は用意してよいが、既定は落ちる側に置き、`--allow-skip` のように明示したときだけ飛ばす。段階として通し実行に入れる検査は、入れる前に「その端末で本当に走るか」を、対象を1件わざと壊して落ちることで確かめる。

出どころ：試験A（2026-09-05）

### 台帳の状態欄が対応の記録と食い違う

独立レビューの指摘を台帳で管理するとき、状態欄は着手前の値（作業の波、判断待ち）で始まり、対応が済んだ時点で人が書き改める約束になる。書き改める操作は各項の記録を書く操作と別なので、記録だけ書いて状態欄を置き去りにしても何も起こらない。件数を数えると未了が多数あるように見え、台帳が件数の正本として使えなくなる。

試験Aでは、221件すべての結果が埋まった後も153件が着手前の値のまま残っていた。そのずれを根拠に、別の判断文書が「この指摘は未決である」という誤った前提を書き、単独で決められる件を他の件の帰結待ちとして先送りしていた。参照する側は状態欄を読むだけなので、更新漏れをそのまま引き継ぐ。

結果が書かれているのに状態が未完了の項、状態が完了なのに対応の記録が1行も無い項、状態欄の値が定めた語彙の外にある項を機械で検出する。節の頭に件数を書くなら、実際の項目数との照合も同じ検査に入れる。対応の記録を示す鍵は台帳によって違うので（結果・対応・再検証）、1つの鍵だけを見て「無ければ未対応」と決め打たない。

出どころ：試験A（2026-09-06）

### 振り直された識別子の残骸を、現存の欠陥と読む

点検の記録が「この図表が指す解析IDが結果データに無い」と書いていても、その解析IDが以前の版で使われていた番号で、指していた図表そのものが別の番号へ振り直された後の残骸であることがある。記録は当時の状態としては正しく、番号だけが今の実体と対応しない。現存の欠陥として扱うと、無いものを作る作業が発生する。

試験Aでは、判断文書がこの記録を裏付けとして引き、関係の無い件の根拠にしていた。

識別子の食い違いを記録するときは、記録した日と、その時点で識別子がどちらへ動いたかを併せて書く。番号を振り直したら、その番号を挙げている記録を検索し、当時の状態であることを追記する。古い記録を根拠に引くときは、そこに出る識別子が今も同じものを指すかを先に確かめる。

出どころ：試験A（2026-09-06）

### 再現の失敗を、環境ではなく成果物の欠けと読む

図が描けない失敗を見て、出力先のフォルダが無いことを原因と決め、フォルダを作って再走したが同じ失敗だった。原因は描画デバイスの系統ライブラリの不在で、エラー文（コネクションを開けない）は直接の原因を示していなかった。最初の見立てを所見に書いた後で訂正することになった。

再現の失敗は、直す前に最小の再現（当該関数を単独で呼ぶ）で原因を確かめる。エラー文が指す箇所（ファイルを開く）の一つ前（ファイルを作る側）を疑う。

出どころ：試験A（2026-09-06）

## 納品と再現で壊れるもの


### 配布経路が先頭ドットのファイルを落とす

納品パッケージの組み立ては全ファイルを書き、同梱物の一覧（manifest）にも載るが、クラウドストレージの同期や zip の展開が先頭がドットのファイルを黙って落とす。受け取った側では一覧に在るファイルが無く、それが R の起動設定なら renv が有効にならないまま手順が進む。

試験Aで、Box へ置いた納品パッケージの `reproduce/` から `.Rprofile` だけが消えていた。組み立てた端末の検査は通っており、消えたのは置いた後である。

置いた先を実体で数える（一覧の件数と置き場の件数）。納品物に先頭ドットの名前を使わず、起動に要る設定は普通の名前のファイルが明示して読む形にする。

出どころ：試験A（2026-09-06）

### 描画デバイスが renv の外の系統ライブラリに依存する

R のパッケージの版は renv.lock が固定するが、`grDevices::svg()` が使う cairo は R 本体の側にあり、macOS の CRAN 版 R では XQuartz が無いと読み込めない。図を描く段だけが「コネクションを開くことができません」で落ち、原因（デバイスが開けず一時ファイルが作られない）はエラー文から読めない。表は全部できるので、成果物の欠けとして現れる。

試験Aの再現で、XQuartz の無い Mac では図4件が作れず TLF 段が異常終了した。Windows の CRAN 版 R は cairo を同梱するので、組み立てた端末では起きていなかった。

再現手順の「用意するもの」に renv.lock が固定しない前提（描画デバイス・フォント・OS）を書く。納品前に組み立てた端末と別の OS で一度回す。

出どころ：試験A（2026-09-06）

### 改行コードで納品物のハッシュが端末ごとに変わる

git の作業ツリーが Windows では CRLF、macOS では LF になるため、同じコミットから組み立てた納品パッケージでもテキストファイルの SHA-256 が違う。manifest による同一性の照合が、組み立てた端末が違うだけで不一致になる。

試験Aで、同じコミットから2つの OS で組み立てた 602 ファイルのうち 29 ファイルの SHA が違い、改行を正規化すると実行環境の記録以外はすべて一致した。

`.gitattributes` で改行を LF に固定する。照合は改行を正規化してから行うか、正規化済みであることを前提にできる状態を作る。

出どころ：試験A（2026-09-06）

### 手順書の成功判定が、同梱しない系統のファイルを前提にする

再現手順は「突合プログラムがすべて正常終了すれば同じ」と書くが、突合プログラムが両系統の判定ファイルを探す作りで、片方の系統を同梱しない納品物では常に異常終了する。結果値の全行が一致していても、受け取った側は成功と読めない。

試験Aで、ARD の突合が 30,755 行すべて一致したうえで、SAS 系の判定ファイルの不在を理由に終了コード1を返した。手順書には別の既知の失敗（SDTM の外に置いたドメイン）の断りだけがあった。

突合プログラムを納品物の中で一度回し、終了コードと出力を手順書の成功判定と突き合わせる。同梱しないものへの依存は、納品物の中では検査から外すか、断りを書く。

出どころ：試験A（2026-09-06）

## 規定と実装の間で壊れるもの


### 単位の再ラベルと閾値の判定が別の層にあり、規定の閾値が当たらない

一次文書が測定キットごとの単位と閾値を規定していても、SDTM の層で単位を付け替える処理と、ADaM の層で区分を判定する処理が別々に書かれていると、判定の側が単位を見ずに1つの閾値を当てる。規定は文書にあり、単位も付いているのに、両方を繋ぐコードがどこにも無い。

試験Aで、測定機関が特定のキットの行に % の単位を付けていたが、ND・NQ・DT の判定は全行に copies の閾値を当てていた。1例の3時点の区分が動く。

判定の関数が単位を引数に取る形にする。一次文書の「〜の場合は」で始まる規定を全部列挙し、それぞれがコードのどこで実装されているかを対応表にする。対応の無い規定は未実装である。

出どころ：試験A（2026-09-06）

### シート名の型で評価時点を絞ると、別名のシートが黙って外れる

評価時点を `LBSPID` が `evaluation*` のような名前の型で選ぶと、同じ性質の測定でも別名のシート（随時提出の報告、補助療法中の達成報告）が判定の対象から外れる。外れた行は判定の属性が空になるだけで、警告は出ない。

試験Aで、補助療法中の初回 CMR 達成報告シートで到達した1例が、判定の対象外だったため未到達として数えられた。到達例数が1違う。

評価時点の対応表に「判定に使うか」の列を持ち、対応表に無いシートが測定値を持っていたら止める。名前の型で選ばない。

出どころ：試験A（2026-09-06）

### 一次文書の内部不整合を、実装が片側を採ったことで解消済みと読む

SAP の本文と図表案、あるいは研究計画書と SAP の間で規定が食い違うとき、実装がどちらかを採って決定記録に残せば作業は進む。しかし文書の側は直っていないので、後から一次文書と実装を突き合わせる者には「規定と実装が違う」と見える。決定記録は納品物に入らないことが多い。

試験Aで、CMR の判定経路（測定値からか、CRF の判定値からか）、移植後 TKI 投与の表の対象（移植例を含むか）、未検の症例の区分（評価不能か未施行か）の3件がこの型だった。いずれも決定記録はあり、SAP の文言は残っていた。

食い違いを見つけたら、決定と同時に「SAP を改訂する」か「逸脱として総括報告書に書く」かを決め、どちらかの記録を納品物に入る文書へ置く。

出どころ：試験A（2026-09-06）

### 決定の記録が複数の文書に分かれ、独立に古くなる

一次文書の空白を埋めた決定と逸脱を、仕様書の付録・決定記録・総括報告書の対応表・機械可読の台帳に分けて書くと、役割分担がどこにも書かれないまま同じ決定が複数箇所に載る。値が変わったとき全部を直せず、片方だけが古くなる。総括報告書に何を書くかの正本が古い規則を載せていても、機械検査はすべて通る。

試験Aで、決定が5箇所に分かれ、主要評価項目の信頼区間の形式は6箇所にあった。分母の規則を改めたとき、総括報告書の対応表だけが旧規則のまま5日間残った。納品パッケージに入る29件の md のうち22件が一次文書の空白か食い違いに触れる記述を持っていた。

決定の正本を1ファイルに決め、他の仕様書は識別子かリンクで引く形にする。仕様書が日付付きの決定を述べていて台帳を引いていない箇所を検査で落とす。

出どころ：試験A（2026-09-06）

### レビューの Critical の定義に、決定済みを除く条項が無い

独立レビューへ「一次文書の規定と実装が食い違っている」を無条件に Critical として渡すと、意図して決めた逸脱が毎回上がる。判断待ちの事項だけを除外しても足りない。決定済みのものが除外されないためである。

試験Aの第4回で、13件の Critical のうち1〜2件がこれにあたった。決定は記録されていたが5箇所に分かれており、レビューは台帳を3つ当たって空だった。指摘の側の誤りではなく、記録の側の欠陥が指摘として現れた形である。

除外を2つ書く。判断待ちとして掲げてある事項と、決定済みとして記録してある事項である。決定済みのものを見つけたときは捨てさせず、台帳のどのエントリが持つか、記述が現状と合っているか、総括報告書での扱いが決まっているかを報告させる。3つ目が未定なら有効な指摘になる。

出どころ：試験A（2026-09-06）

### 節の振り分け表の名前が見出しと1文字違い、移動元を消した時点で節が消える

1つの文書を複数へ分けるとき、行き先を節の名前で表に持つと、表の名前が実際の見出しと合っていない節が行き先を持たない。移動先へ書いたあと移動元を消す作りだと、その節はどこにも残らない。移動先の合計行数を見ても、元より少ないことに気づけない。

試験Aで、振り分け表が「CRF 項目と SDTM の対応」と書き、実際の見出しが「CRF と SDTM の対応」だったため36行の節が失われた。git に入っていたので復旧できた。

振り分けは、割り振らなかった節を必ず一覧で出し、空であることを確かめてから移動元を消す。移動元の削除は、振り分けが尽きたことの確認と同じ実行の中で行う。

出どころ：試験A（2026-09-06）

### 生成の一覧が docs 相対のパスを持ち、文書を移すと黙って納品物から外れる

納品物へ入れる文書の一覧と、HTML へ変換する対象の一覧を `<フォルダ>/<名前>` の形で持つと、文書を別のフォルダへ移したときに一覧が外れる。実在しないものを飛ばす作りだと、移した文書が納品物から落ちたまま組み立てが成功する。

試験Aで、仕様14件を別のフォルダへ移した際に2つの一覧が古いままになった。気づけたのは、同梱物の中から参照先の無いリンクが3件出てビルドが止まったためで、リンクを持たない文書が落ちていたら気づけなかった。

一覧はフォルダを含まない基底名で持ち、`docs/` を横断して解決する形にする。解決できない名前があれば止める。基底名の重複は禁じる。

出どころ：試験A（2026-09-06）
