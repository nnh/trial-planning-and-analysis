# trial-planning-and-analysis Action Items

作成日：2026-09-13
改訂日：2026-09-13

## 次の試験に入る前に片づけるもの

試験Aを1本通した知見が本リポジトリへ取り込めているかを 2026-09-13 に監査した結果、型と設計原則は入っている一方、実行できる形と試験を始める入口が無いと分かった。次の試験は統計解析計画書の固定前（区間0）にいるので、1と2を先にする。

- [ ] **実行の形を `pipeline/scripts/` へ持ち込む。** 段階の順序はスクリプトを正本にすると決めた以上、通し実行のスクリプトが無いと順序そのものが本リポジトリに存在しない。試験側には Python の通し実行（7段階）と共通処理、それが呼ぶ4本のランナー、define の表示用 HTML の生成がある。`pipeline/scripts/powershell/` の6本はこれに置き換わったもので、`pipeline/README.md` 自身が「持ち込むまで新しい試験の出発点にしない」と書いている。持ち込みは「試験側との同期」の手順で行う（枠組み側の汎用化を控えてから戻す）
- [ ] **検査スクリプト8本を `pipeline/scripts/python/` へ足す。** 決定の正本を守る検査、レビュー台帳の検査、宣言と図表の対応の検査、交換形式の型宣言の検査、識別子の格納長の検査、SDTM の宣言の検査、視覚回帰、ADaM の論理検証。とくに決定の正本を守る検査は `pipeline/analysis-pipeline-plan.md`「検査で守る」が前提にしているのに実体が無い
- [ ] **宣言の雛形18件を `templates/` へ足す。** 区間0の出口条件は「宣言の CSV が全部そろい」だが、雛形は8件しか無い。足りないのは受入基準3本（主要評価項目・解析対象集団の条件・図表ごとの期待）、症例報告書の対応2本、SDTM のコードリスト系4本、解析の群と目的と手法の3本、データセットのラベル、ADaM のコードリスト2本、適合性検証の指摘の仕分け、参照文書、納品の連絡先。受入基準3本は区間0の出口条件そのものなので最初に入れる。あわせて `templates/README.md` に列の定義を書く
- [ ] **試験Aの通過記録を `trials/` へ起こす。** `trials/README.md` は「まだ登録は無い」のままで、枠組みが「効いている唯一の証拠」と呼ぶ記録が1本目について存在しない。書くのは決定の件数と区分×決め方の内訳（78件。補完かつ固定データ42・補完かつ文書のみ13・逸脱かつ固定データ5・逸脱かつ文書のみ7・解消10・観察1）、区間1で出た判断票の件数と「区間0の漏れ」の件数、区間2を回った回数、最終レビューの Critical の件数、機械検査が拾った件数。`pipeline/analysis-pipeline-plan.md` の「適用例」「試験実績」が持つ数値はここへ移し、設計文書からは落とす
- [ ] **固定の工程の3点セットを揃える。** 手順は `review/sap-review/data-verification.md` にあるが、機械検査と欠陥事例が無い。試験Aは固定後に再抽出を3回受けており、受けるたびに受領データの直下を差し替えて全段を回し直し、どの版に基づく結果かを総括報告書に述べると決めた。`data-verification.md` にあるのは「データカットが更新されたとき再実行する」の1行だけなので、差し替えの受け方・版の追い方・照会の原本の保存先・提出のたびに増える日付フォルダの追い方を足す
- [ ] **納品より後の工程を1つ足す。** 枠組みは立案・構築・固定・解析・納品の5工程で閉じているが、試験Aでは納品の後に、終了届書と審査資料に用いる総括報告書概要の作成、追加解析の受け方、重篤な有害事象の経過記述の管理記録が発生した。決定記録の総括報告書欄の設計は入っているが、概要書類・研究代表医師のレビュー・審査提出・追加解析の受け方の段が無い。追加解析については、解析用データセットを渡さず依頼で受けるという判断と、比例ハザードを頼まれたときに先に確かめる3点（解析対象集団で絞る、イベント数が共変量の数を決める、競合リスクの推定量に素のモデルを当てない）が素材になる
- [ ] **新しい試験を始める手順を1本書く。** `templates/README.md` に「雛形を `docs/` へコピーする」とあるだけで、リポジトリの骨格（`CLAUDE.md`・`docs/` の区分・実装系統を対等に置く `program/`・`scripts/`）を起こす手順が無い。次の試験はこれを手で起こしている。リポジトリの骨格、雛形のコピー、スクリプトのコピー、試験固有の値の記入、立案時レビューの実施、固定前に決め切る12事項の決定、の順で書く
- [ ] **蓄積の追記トリガーを配布元へ入れ直す。** `findings/README.md` が定める「育つ仕組み」は、セッション終了のたびに工程ごとの欠陥事例へ追記されることで回る。2026-08-29 にセッション終了ルーチンへその段を足した記録があるが、配布元へ反映しないまま上書きされて現存しない。起点が人の記憶にしか無い状態になっている
- [ ] **試験側との同期の遅れを取り戻す。** スクリプトの同期は 2026-08-29、文書の反映は 2026-09-07 が最後で、その後の知見が入っていない。`pipeline/analysis-pipeline-plan.md`「同期を溜めない」に自ら反している。あわせて `findings/README.md` の「固定・納品の欠陥事例はまだ無い」という記述が `findings/analysis-findings-log.md` の「納品と再現で壊れるもの」の節と食い違っているので直す
